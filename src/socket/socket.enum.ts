export enum GameChallengeTitle {
  Trivia = 'trivia',
}

export enum ChallengeResponseType {
  Accepted = 'accepted',
  Rejected = 'rejected',
}

export enum GameChallengeCommand {
  NEXT = '/next',
  HINT = '/hint',
  STOP = '/stop',
}
